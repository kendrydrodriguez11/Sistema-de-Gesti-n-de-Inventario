logging:
  level:
    root: INFO
    software:
      amazon:
        awssdk: DEBUG

server:
  port: 8082

spring:
  application:
    name: microservice-aws

  aws:
    secret:
      key: ${AWS_SECRET_KEY}
    access:
      key: ${AWS_ACCESS_KEY}
    region: ${AWS_REGION:us-east-1}
